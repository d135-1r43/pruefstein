quarkus.rest-client.pruefstein-api.url=http://localhost:8080
quarkus.jackson.write-dates-as-timestamps=false

# Disable scheduler by default — use the 'run' CLI command or configure a system cron job
quarkus.scheduler.enabled=false

# ── Device authorization grant (user login) ─────────────────────────────────
pruefstein.agent.auth-server-url=http://localhost:8180/realms/pruefstein
pruefstein.agent.client-id=pruefstein-web

# ── Production overrides ────────────────────────────────────────────────────
%prod.pruefstein.agent.auth-server-url=${KEYCLOAK_URL}/realms/pruefstein
